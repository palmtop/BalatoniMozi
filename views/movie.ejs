<!DOCTYPE html>
<html lang="hu">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= movieName %></title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 1rem;
      background: #eef2f5;
    }
    h1 {
      text-align: center;
      margin-bottom: 1rem;
    }
    .card {
      background: white;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
      padding: 1rem;
      margin-bottom: 1rem;
    }
    .card h2 {
      font-size: 1.2rem;
      margin-top: 0;
      color: #0077aa;
    }
    .card ul {
      list-style: none;
      padding-left: 0;
    }
    .card li {
      padding: 0.2rem 0;
    }
  </style>
</head>
<body>
  <p><a href="/"><i class="fas fa-home"></i></a></p>
  <h1><%= movieName %></h1>
  <div class="card">
    <h2>Dates and Theaters</h2>
    <ul>
      <% schedule.forEach(movie => { %>
        <li><strong><%= movie.date %>:</strong> <%= movie.theater %></li>
      <% }); %>
    </ul>
  </div>
</body>
</html>