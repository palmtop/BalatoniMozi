<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Page</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1>Movie Metadata Download</h1>

    <p>This is the administrative page for downloading movie metadata.</p>

    <% if (messages) { %>
        <div class="messages">
            <% messages.forEach(function(message) { %>
                <p><%= message %></p>
            <% }); %>
        </div>
    <% } %>

    </div>
</body>
</html>